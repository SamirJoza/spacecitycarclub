// resources/js/blocks/my-block.js
// Minimal block script: keeps a reference for potential DOM behavior
document.addEventListener('DOMContentLoaded', () => {
  document.querySelectorAll('.my-block').forEach((el) => {
    // placeholder: you can wire interactivity here
  });
});
